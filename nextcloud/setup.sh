#!/usr/bin/env bash

DATA_DIR="$HOME/docker-data/nextcloud"

mkdir -p "${DATA_DIR}/mariadb"
mkdir -p "${DATA_DIR}/redis"
mkdir -p "${DATA_DIR}/html"
mkdir -p "${DATA_DIR}/config"
mkdir -p "${DATA_DIR}/apps"
